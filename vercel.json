{
  "buildCommand": "pnpm --filter @easyfinderai/shared build && pnpm --filter @easyfinderai/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/"
    }
  ]
}
